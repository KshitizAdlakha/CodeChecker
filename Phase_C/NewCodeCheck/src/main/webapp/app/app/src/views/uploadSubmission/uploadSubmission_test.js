'use strict';

describe('codeChecker.uploadSubmission module', function() {

    beforeEach(module('codeChecker.uploadSubmission'));

    describe('uploadSubmission controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var UploadSubmissionCtrl = $controller('UploadSubmissionCtrl');
            expect(UploadSubmissionCtrl).toBeDefined();
        }));

    });
});
'use strict';

describe('codeChecker.help module', function() {

  beforeEach(module('codeChecker.help'));

  describe('Help controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var HelpCtrl = $controller('HelpCtrl');
      expect(HelpCtrl).toBeDefined();
    }));

  });
});
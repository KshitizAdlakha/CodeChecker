'use strict';

describe('codeChecker.sideBySide module', function() {

    beforeEach(module('codeChecker.sideBySide'));

    describe('sideBySide controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var SideBySideCtrl = $controller('SideBySideCtrl');
            expect(SideBySideCtrl).toBeDefined();
        }));

    });
});